<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
</head>
<body>
<h1>Корзина товаров</h1>
<div id="cartProducts-container"></div>
<ul>
    {% for product in cartProducts.product %}
        <li><p>Название: {{ product.dataValues.name }}</p> </li>
        <li><p>Цена: {{ product.dataValues.price }}</p></li>
        <li><p>Артикул: {{ product.dataValues.id }}</p></li>

    {% endfor %}
</ul>
<form id="productForm" method="post">
    <button type="submit">Оплатить</button>
</form>
</body>
</html>
